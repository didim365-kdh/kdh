{
  "name": "my-nodejs-app",
  "version": "1.0.0",
  "description": "Node.js app for Elastic Beanstalk",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=build/test-results/test-results.xml"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.0"
  }
}
